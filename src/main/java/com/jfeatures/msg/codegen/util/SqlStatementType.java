package com.jfeatures.msg.codegen.util;

/**
 * Enum to represent different types of SQL statements supported by the code generator.
 */
public enum SqlStatementType {
    SELECT,
    UPDATE,
    INSERT,
    DELETE,
    UNKNOWN
}